// For Loop
let Table = parseInt(prompt("Enter the Number for table : "));
for (let i = 0; i <= 10; i++) {
  // console.log(Table + " x " + i + " = " + (Table * i));
  console.log(`${Table} x ${i} = ${Table * i}`);
}


// While loop
let Number = int(prompt("Enter the num :")) 
// Number = Number.parseInt(Number)
let i=0;
while(i<Number)
{
  console.log(i);
  i++
}
